import { createTheme } from '@mantine/core';

export const mantineTheme = createTheme({
  primaryColor: 'cyan',
  primaryShade: 8,
  // colors: {
  //   primary: virtualColor({
  //     name: 'grayish',
  //     dark: 'gray.1',
  //     light: 'gray.9',
  //   }),
  // },
});
